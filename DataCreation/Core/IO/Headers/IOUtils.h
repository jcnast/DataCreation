#pragma once

#include "IODefs.h"
#include "File.h"

namespace Core
{
	namespace IO
	{
		File OpenFile(FilePath file);
		File OpenFile(FilePath file);
		File OpenFile(FilePath file);

		String GetCWD();
	}
}